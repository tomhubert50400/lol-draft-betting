{
    "name": "lol-draft-betting",
    "private": true,
    "version": "0.0.0",
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "lint": "eslint .",
        "preview": "vite preview",
        "deploy": "npm run build && npx firebase deploy --only hosting",
        "deploy:all": "npm run build && npx firebase deploy --only hosting,firestore:rules",
        "deploy:rules": "npx firebase deploy --only firestore:rules",
        "deploy:functions": "npx firebase deploy --only functions",
        "deploy:web": "npm run build && npx firebase deploy --only hosting,firestore:rules"
    },
    "dependencies": {
        "firebase": "^10.14.1",
        "react": "^18.2.0",
        "react-dom": "^18.2.0",
        "react-router-dom": "^6.22.0"
    },
    "devDependencies": {
        "@types/react": "^18.2.56",
        "@types/react-dom": "^18.2.19",
        "@vitejs/plugin-react": "^4.2.1",
        "eslint": "^8.56.0",
        "eslint-plugin-react": "^7.33.2",
        "eslint-plugin-react-hooks": "^4.6.0",
        "eslint-plugin-react-refresh": "^0.4.5",
        "firebase-tools": "^13.0.0",
        "vite": "^5.1.4"
    }
}
